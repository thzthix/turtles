[project]
name = "turtle-behavior-agent"
version = "0.1.0"
description = "바다거북 행동 분석 AI 에이전트 (Sea Turtle Behavior Analysis AI Agent)"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai>=0.0.24",
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]
